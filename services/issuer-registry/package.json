{
  "name": "@passport/issuer-registry",
  "version": "0.0.0",
  "private": true,
  "description": "Trusted-issuer registry: admission process, accreditation levels, published governance model, revocation.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "@passport/service-kit": "workspace:*",
    "@passport/shared-types": "workspace:*",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
