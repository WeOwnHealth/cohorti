{
  "name": "@cohorti/issuance",
  "version": "0.0.0",
  "private": true,
  "description": "Sign and issue credentials carrying issuer identity, accreditation level, observation date, and binding tier.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "@cohorti/service-kit": "workspace:*",
    "@cohorti/shared-types": "workspace:*",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
