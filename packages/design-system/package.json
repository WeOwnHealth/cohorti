{
  "name": "@cohorti/design-system",
  "version": "0.0.0",
  "private": true,
  "description": "Shared design tokens (tokens.css) and UI primitives for every Cohorti frontend surface, synthesized from attio.com and notion.com's live CSS.",
  "type": "module",
  "exports": {
    "./tokens.css": "./tokens.css",
    "./components": "./src/index.tsx"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured yet)\""
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "typescript": "^5.7.2"
  }
}
