pragma language_version 0.23;

import CompactStandardLibrary;

witness biomarkerValue(): Uint<64>;

export circuit proveWithinRange(min: Uint<64>, max: Uint<64>): Boolean {
  return disclose(biomarkerValue() >= min && biomarkerValue() <= max);
}
