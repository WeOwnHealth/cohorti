{
  "name": "@cohorti/contracts-evm",
  "version": "0.0.0",
  "private": true,
  "description": "ERC-7857 agent identity + audit-digest anchor contracts. Foundry project — see README.md.",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "clean": "forge clean",
    "typecheck": "echo '(Solidity has no separate typecheck step — see the build script)'",
    "lint": "forge build --lint"
  }
}
